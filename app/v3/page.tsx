import { PetsFableView } from "@/components/premium-v3/pets-fable";

/** Landing V3 "Fable", preservada tras el rediseño total del index (V4). */
export default function Page() {
  return <PetsFableView />;
}
